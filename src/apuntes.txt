const bienvenido = JSON.parse(fs.readFileSync('./database/json/bienvenido.json'))
const ffmpeg = require('fluent-ffmpeg')

// ◉ | ¿Como chucha uso el bot? |
//   ║
//   ╚ ○  Registrate con el comando ${prefijo}cpp y tu nombre


// ◉ | CONVERTIDORES |
//   ║     
//   ╠ De Sticker a JPG
//   ╠ ○  ${prefijo}toimg
//   ║
//   ╠ De Video a MP3
//   ╚ ○  ${prefijo}tomp3

const getRandom = (ext) => {
    return `${Math.floor(Math.random() * 10000)}${ext}`
}

// else if (msg.body === `${prefijo}mipremio`) {

            //     const i = Math.floor(Math.random() * (6 - 1)) + 1;
            //     const media = MessageMedia.fromFilePath(`src/assets/img/porno${i}.jpeg`);
            //     cliente.sendMessage(msg.from, media);
            // }

            else if (msg.body.toLowerCase().includes('bot')) {

                const i = Math.floor(Math.random() * (5 - 1)) + 1;

                const media = MessageMedia.fromFilePath(`src/assets/audio/anime${i}.mp3`);
                cliente.sendMessage(msg.from, media);
            }


app.get('/index', (req, res) => {
    res.render('index');
});


https://graph.facebook.com/102207198205289/subscribed_apps?subscribed_fields=live_videos&access_token=EAAJwZAiJd21IBAGPDweGzuvv3q8YlbquLP5XbZCebHmJCTPmxQ3wH6poCx2wpLlZAk1TQB4lWUYgA5DcR54ZBOcLBVOs3OBJ9RhJOvHz93yR46wLqvkCvmJQqW0GfAaOtY3OiR6wEFVdUcUZCjxY8LLzHgTihZBdWAoo76GzCI6ZALFeZB3tW769PI0h9KYJiFZAcUDqLIxiWWwZDZD


// if (req.body.embeds) {
    //     startBot(req.body)
    // }

    // if (req) {
        //     cliente.sendMessage(grupoGeneral, req.embed[0].description);
        //     cliente.sendMessage(grupoProgra, req.embed[0].description);
        // }